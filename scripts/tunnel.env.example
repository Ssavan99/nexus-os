# Phase 2D.2 tunnel config — copy to scripts/tunnel.env (gitignored) and fill in.
# Your reserved ngrok static domain (from the ngrok dashboard -> Domains).
STABLE_DOMAIN=your-name.ngrok-free.app

# Optional overrides (defaults shown):
# PORT=3131
# GBRAIN_BIN=/Users/ssavan99/.bun/bin/gbrain
# NGROK_BIN=/opt/homebrew/bin/ngrok
